# DETR checkpoint (placeholder for MS4)

This directory will hold the DETR baseline's trained checkpoint when the
work on the `feature/DERT` branch is integrated into `main` (planned for
MS4).

The DETR work currently lives on the `feature/DERT` branch
(`YA_MS3.ipynb`). The trained checkpoint is in Yoseph's Google Drive,
referenced in that notebook. When MS4 picks up DETR for end-to-end
density evaluation, the checkpoint will be placed here under a name
like `best_detr.pt`.

Empty for MS3.
